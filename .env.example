# Application/runtime connection used by PrismaPg
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Direct connection used by Prisma CLI migrations and administration
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
